spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/travinh_realty_test}
    username: ${DB_USERNAME:travinh_test}
    password: ${DB_PASSWORD:test-only-change-this-postgres-password}
  jpa:
    hibernate:
      ddl-auto: validate

app:
  jwt:
    secret: ${JWT_SECRET:test-only-jwt-secret-that-is-at-least-thirty-two-bytes}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
