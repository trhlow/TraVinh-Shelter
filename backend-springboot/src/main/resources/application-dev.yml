spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/travinh_realty}
    username: ${DB_USERNAME:travinh_dev}
    password: ${DB_PASSWORD:dev-only-change-this-postgres-password}

app:
  jwt:
    secret: ${JWT_SECRET:replace-this-local-development-secret-with-a-long-random-value}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
